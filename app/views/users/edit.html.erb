<%= form_for @user do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>Please fix <%= pluralize(@user.errors.count, "error") %>:</h2>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-content">
  <div class="row">
    <div class="centered">
      <% email = nil if local_assigns[:email].nil? %>
      <% if @user.active %>
        <div class="field">
          <p class = 'desc-field'>Username</p>
          <%= f.text_field :username, placeholder: "Name" , class:"imput-field"%>
        </div>
      <% end %>
      <div class="field">
        <p class = 'desc-field'>Email</p>
        <% if email %>
          <%= f.text_field :email, value: email %>
        <% else %>
          <%= f.text_field :email, placeholder: "Email",class:"imput-field" %>
        <% end %>
      </div>
      <% if @user.active %>
        <div class="field">
          <p class = 'desc-field'>Phone</p>
          <%= f.text_field :phone, placeholder: "Phone",class:"imput-field" %>
        </div>
      <% end %>
      <div class="field">
        <p class = 'desc-field'>Password</p>
        <%= f.password_field :password, placeholder: "Password",class:"imput-field" %>
      </div>
      <div class="actions">
        <% if @user.active %>
         <%= submit_tag 'Save', class: "edit-button" %>
        <% else %>
          <%= submit_tag 'create account', class: "edit-button" %>
        <% end -%>
      </div>
   </div>
  </div>
</div>

<% end %>
