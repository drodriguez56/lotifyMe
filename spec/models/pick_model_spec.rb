require 'rails_helper'

describe Pick do
   it "is valid with numbers, game, draw_date"
   it "is invalid without numbers"
   it "is invalid without game"
   it "is invalid with draw_date"
   it "does not allow duplicate emails"
end